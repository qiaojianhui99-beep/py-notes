# 运算符

## 算术运算符

```python
a, b = 10, 3

a + b   # 13  加法
a - b   # 7   减法
a * b   # 30  乘法
a / b   # 3.333... 除法
a // b  # 3   整除
a % b   # 1   取余
a ** b  # 1000 幂运算
```

## 比较运算符

```python
a == b  # False 等于
a != b  # True  不等于
a > b   # True  大于
a < b   # False 小于
a >= b  # True  大于等于
a <= b  # False 小于等于
```

## 逻辑运算符

```python
True and False  # False
True or False   # True
not True        # False
```

## 赋值运算符

```python
x = 10
x += 5   # x = x + 5
x -= 3   # x = x - 3
x *= 2   # x = x * 2
x /= 4   # x = x / 4
```

## 成员运算符

```python
"a" in "apple"      # True
"z" not in "apple"  # True
```

## 身份运算符

```python
a = [1, 2, 3]
b = a
c = [1, 2, 3]

a is b      # True  (同一对象)
a is c      # False (不同对象)
a == c      # True  (值相等)
```

## 运算符优先级

从高到低：
1. `**` (幂运算)
2. `*`, `/`, `//`, `%`
3. `+`, `-`
4. `==`, `!=`, `>`, `<`, `>=`, `<=`
5. `not`
6. `and`
7. `or`

## 使用场景

### 场景 1：数学计算
科学计算、金融系统、游戏开发中的数值运算。

### 场景 2：条件判断
用户权限验证、数据范围检查、表单验证。

### 场景 3：数据过滤
列表筛选、数据库查询条件、搜索功能。

## 练习题

### 基础练习

**题目 1**：计算：`(10 + 5) * 3 // 2 ** 2` 的结果是多少？

<details>
<summary>💡 查看答案</summary>

```python
result = (10 + 5) * 3 // 2 ** 2
print(result)  # 11
```

**解析**：`2 ** 2 = 4` → `15 * 3 = 45` → `45 // 4 = 11`
</details>

**题目 2**：判断一个数 `x` 是否在 10 到 20 之间（包含边界）。

<details>
<summary>💡 查看答案</summary>

```python
x = 15
if 10 <= x <= 20:
    print("在范围内")
```
</details>

### 进阶练习

**题目 3**：判断年份是否为闰年（能被 4 整除但不能被 100 整除，或能被 400 整除）。

<details>
<summary>💡 查看答案</summary>

```python
year = 2024
is_leap = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)
print(is_leap)  # True
```
</details>

### 挑战练习

**题目 4**：实现一个计算器函数 `calculate(a, op, b)`，支持 `+`, `-`, `*`, `/`, `//`, `%`, `**` 运算符。

## 费曼学习法检验

用自己的话回答以下问题：

1. **这是什么**：`/` 和 `//` 有什么区别？为什么需要两种除法？

2. **为什么需要**：为什么 `a is b` 和 `a == b` 的结果可能不同？

3. **怎么用**：向新手解释，如何检查一个数是否是偶数？

4. **注意事项**：`and` 和 `or` 的短路特性是什么？有什么实际用途？

::: tip 学习建议
运算符优先级很重要！不确定时使用括号 `()` 明确优先级，提高代码可读性。
:::
